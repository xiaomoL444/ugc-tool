/** Legacy entry point: hiding a picker entry must never remove imported animation data. */
export function removeRetiredScaleTweenTracks(project: Record<string, unknown>): number {
  void project;
  return 0;
}
