# ugc-tools i18n 规范与接入指南

更新日期：2026-09-19。

本文以 **SoundEffectPlayer（音效播放器）** 当前实现为标准，规定本地固定文案、远程资产翻译、数据索引和页面调用的组织方式。新页面和后续迁移应采用这一套结构。

特效播放器的旧远程格式属于历史兼容，计划后续整理，**不作为本规范的接入模板**。当前加载器仍兼容旧格式，不代表新资源应继续使用它。

文中区分“规范”和“当前实现”：规范用于约束后续维护；当前实现说明代码已经具备的行为和限制。JSON 示例中的资源条目仅为节选，不可直接覆盖完整资源库。

## 目录

1. [基本原则](#1-基本原则)
2. [目录和语言代码](#2-目录和语言代码)
3. [翻译键命名](#3-翻译键命名)
4. [本地固定文案](#4-本地固定文案)
5. [远程资产词库](#5-远程资产词库)
6. [数据索引](#6-数据索引)
7. [页面接入](#7-页面接入)
8. [语言选择和切换](#8-语言选择和切换)
9. [远程加载规则](#9-远程加载规则)
10. [开发与生产地址](#10-开发与生产地址)
11. [搜索、状态和缺译](#11-搜索状态和缺译)
12. [维护与扩展流程](#12-维护与扩展流程)
13. [验证与测试](#13-验证与测试)
14. [常见问题](#14-常见问题)
15. [代码入口与历史兼容边界](#15-代码入口与历史兼容边界)

## 1. 基本原则

项目共用一个 Vue I18n 实例 `i18n` 和一个远程加载器 `remoteI18n`。页面通过命名空间区分文案，不单独创建另一套语言状态。

国际化数据分为三部分：

| 内容 | 音效播放器示例 | 存放位置 |
| --- | --- | --- |
| 固定界面文案 | 选择音效、搜索、播放、音量、加载失败 | 本地 `src/i18n/locales/soundEffectPlayer/<语言>.json` |
| 资产名称 | 环境_震动、环境分类 | 远程 `SoundEffectPlayer/i18n/<语言>.json` |
| 业务数据 | 音效 ID、时长、分类关联、排序 | 远程 `SoundEffectPlayer/data.json` |

规范如下：

- 本地和远程词库都使用 **JSON 完整键字典**，即 `"完整键": "译文"`。
- 每个文件只包含一种语言，不把中英文对象混在同一个词条下。
- 本地固定文案由项目维护；远程资产名称由资产数据维护。
- 业务数据使用稳定 ID，名称字段保存 `nameI18nKey`，不直接存放显示名称。
- 语言切换只改变显示文字和相关搜索结果，不改变资源 ID、分类关联等业务身份。
- 当前语言缺译时显示完整键，不用其他语言或原始中文自动补齐。

**本地与远程文件结构相同，但文本处理方式不同：本地支持消息插值，远程资产按纯文本显示。** 这是为了让固定句子支持 `{count}` 等参数，同时让资产名中的 `@`、花括号、竖线保持原样。

## 2. 目录和语言代码

### 2.1 本地目录

```text
src/i18n/
├── index.ts
├── preferences.ts
├── remote.ts
├── cachedText.ts
├── messageText.ts
└── locales/
    ├── common/
    │   ├── zh-cn.json
    │   └── en-us.json
    ├── soundEffectPlayer/
    │   ├── zh-cn.json
    │   └── en-us.json
    └── <其他页面>/
        ├── zh-cn.json
        └── en-us.json
```

组织顺序为 `locales/页面或公共模块/语言.json`。词库使用 `.json`，不再使用 `export default` 的 `.ts` 文件。`tsconfig.json` 已开启 `resolveJsonModule`。

页面词库放在对应页面目录中；跨页面复用的文案放在 `common` 目录。目录名本身不会自动成为翻译键前缀，例如公共文件仍可同时包含 `app.language` 和 `common.copySuccess`。

### 2.2 远程目录

```text
ugc-tool-data/
└── SoundEffectPlayer/
    ├── data.json
    ├── audio/
    │   ├── 10001.mp3
    │   └── 10002.mp3
    └── i18n/
        ├── zh-cn.json
        └── en-us.json
```

开发资源目录 `ugc-tool-data-beta` 使用相同内部结构。远程词库不放进前端本地词库目录；`data.json` 也不嵌入整份翻译表。

### 2.3 大小写约定

| 用途 | 示例 |
| --- | --- |
| 应用内部语言代码 | `zh-CN`、`zh-TW`、`en-US`、`ja-JP`、`ru-RU` |
| JSON 文件名 | `zh-cn.json`、`zh-tw.json`、`en-us.json`、`ja-jp.json`、`ru-ru.json` |
| 本地模块目录 | `soundEffectPlayer` |
| 翻译命名空间 | `soundEffectPlayer` |
| OSS 工具目录 | `SoundEffectPlayer` |

内部代码使用 `supportedLocales` 中的规范语言值。加载远程文件时，函数会将语言代码转为小写拼接文件名。

OSS 工具目录与命名空间是两个独立参数，不能因为大小写相近就互换。键匹配区分大小写，应保持 `soundEffectPlayer` 的拼写一致。

## 3. 翻译键命名

### 3.1 三类标准键

| 类型 | 规则 | 示例 |
| --- | --- | --- |
| 固定界面文案 | `<namespace>.ui.<语义名>` | `soundEffectPlayer.ui.play` |
| 资产名称 | `<namespace>.data.<稳定ID>` | `soundEffectPlayer.data.10001` |
| 分类名称 | `<namespace>.category.<稳定ID>` | `soundEffectPlayer.category.1` |

固定文案用语义名称，资产和分类用稳定 ID。不要使用显示译文、中文句子或当前列表下标作为键。调整排序、修改名称，不应导致 ID 或翻译键变化。

已有的其他页面可能采用旧键名；新资产应参考音效结构。迁移旧页面时，应同步调整索引、词库和消费代码，而不是只改一边。

### 3.2 使用完整键

本地文件、远程文件、`nameI18nKey` 和组件调用都使用同一个完整键：

```text
data.json:
  nameI18nKey = soundEffectPlayer.data.10001

i18n/zh-cn.json:
  soundEffectPlayer.data.10001 = 环境_震动

组件:
  resourceText(item.nameI18nKey)
```

不要在组件中再次追加 `soundEffectPlayer.`。文件内也不再额外嵌套 `soundEffectPlayer` 对象。

### 3.3 键和值的校验

远程加载器会检查：

- 命名空间是一段名称，不包含点。
- 扁平字典的所有键都以请求的 `<namespace>.` 开头，不能混入其他命名空间。
- 键以点分段，各段允许 Unicode 字母、数字、下划线、连字符。
- 不允许空段，例如 `soundEffectPlayer..play`。
- 不允许 `__proto__`、`prototype`、`constructor` 这些危险段名。
- 不允许父子键冲突，例如同时存在 `soundEffectPlayer.data` 和 `soundEffectPlayer.data.10001`。
- 远程译文只能为字符串或 `null`，不能是数字、布尔值、数组或嵌套对象。

本地也应遵守一致的键命名，但本地 JSON 直接由 Vue I18n 加载，不经过远程解析器。不能假定远程解析器的全部检查都已自动应用到本地。

文件统一采用 UTF-8，键和值使用双引号，不写注释或尾随逗号。避免重复 JSON 键：普通解析通常会保留最后一个值，现有解析器不能可靠提示重复项。

## 4. 本地固定文案

### 4.1 中文与英文示例

`src/i18n/locales/soundEffectPlayer/zh-cn.json`：

```json
{
  "soundEffectPlayer.ui.selectSound": "选择音效",
  "soundEffectPlayer.ui.player": "播放器",
  "soundEffectPlayer.ui.searchLabel": "搜索（名称或 ID）：",
  "soundEffectPlayer.ui.searchPlaceholder": "输入关键词，回车跳到第一项",
  "soundEffectPlayer.ui.soundName": "音效名称：{name}",
  "soundEffectPlayer.ui.resultCount": "{count} 项",
  "soundEffectPlayer.ui.play": "播放",
  "soundEffectPlayer.ui.pause": "暂停"
}
```

`src/i18n/locales/soundEffectPlayer/en-us.json`：

```json
{
  "soundEffectPlayer.ui.selectSound": "Select a sound",
  "soundEffectPlayer.ui.player": "Player",
  "soundEffectPlayer.ui.searchLabel": "Search (name or ID):",
  "soundEffectPlayer.ui.searchPlaceholder": "Enter keywords; press Enter to jump to the first result",
  "soundEffectPlayer.ui.soundName": "Sound name: {name}",
  "soundEffectPlayer.ui.resultCount": "{count} results",
  "soundEffectPlayer.ui.play": "Play",
  "soundEffectPlayer.ui.pause": "Pause"
}
```

两份文件应覆盖相同键，并保留相同参数名称。本地值应为非空字符串，不用 `null` 或空白表示未翻译。

### 4.2 注册方式

`src/i18n/index.ts` 显式导入 JSON：

```ts
import zhCNSoundEffectPlayer from './locales/soundEffectPlayer/zh-cn.json';
import enUSSoundEffectPlayer from './locales/soundEffectPlayer/en-us.json';
```

然后分别展开到语言根对象。以下只是相关配置节选，接入时应保留其他模块和既有配置：

```ts
const options = {
  legacy: false,
  flatJson: true,
  messages: {
    'zh-CN': { ...zhCNSoundEffectPlayer },
    'en-US': { ...enUSSoundEffectPlayer },
  },
};
```

`flatJson: true` 负责将点分完整键展开成 Vue I18n 消息树。不要再写成 `{ soundEffectPlayer: zhCNSoundEffectPlayer }`，否则会重复前缀。

新增 JSON 文件不会自动注册，必须更新导入和 `messages`。当前所有本地词库随应用构建打包，没有按路由动态发现或懒加载本地语言文件。

### 4.3 插值、标点和单位

调用时传入参数：

```ts
t('soundEffectPlayer.ui.soundName', { name: soundName });
t('soundEffectPlayer.ui.resultCount', { count: count });
t('soundEffectPlayer.ui.speed', { speed: speed });
t('soundEffectPlayer.ui.volume', { volume: Math.round(volume * 100) });
```

完整句子、标点和单位尽量放在词库中，让不同语言自行决定语序。不要在代码中先翻译一个片段，再拼接固定中文后缀。

本地文案支持 Vue I18n 消息语法。若未来需要复数形式，可以在消息中用竖线区分，并在 `t(key, params, count)` 中传入复数数量。音效页目前 `resultCount` 使用固定的英文 `results`，没有额外单复数分支。

消息中的 `@`、花括号、竖线若只表示普通字符，需要使用消息字面量转义。例如：

```json
{
  "example.ui.contact": "Contact: name{'@'}example.com",
  "example.ui.literal": "Show {'{'}name{'}'} and {'|'} literally"
}
```

资源名作为 `{name}` 参数传入时，不需要把该参数再次加工为翻译消息。

### 4.4 音效页当前固定文案清单

以下键均位于 `soundEffectPlayer.ui` 下：

| 用途 | 键后缀 |
| --- | --- |
| 区域标题 | `selectSound`、`player` |
| 搜索标签、占位符和数量 | `searchLabel`、`searchPlaceholder`、`resultCount` |
| 全部展开、收起 | `expandAll`、`collapseAll` |
| 卡片 ID 和时长 | `soundDetails` |
| 分类收起提示、空结果 | `collapsedHint`、`empty` |
| 选中音效名称、ID、未选择状态 | `soundName`、`soundId`、`notSelected` |
| 播放控制 | `play`、`pause`、`previous`、`next` |
| 加载状态 | `loading` |
| 速度、音量及说明 | `speed`、`volume`、`volumeHint` |
| 循环开关和间隔 | `loop`、`loopInterval` |
| 分类兜底 | `uncategorized`、`categoryFallback` |
| 失败通知 | `loadFailed`、`playFailed` |

后续增加 tooltip、无障碍标签、弹窗按钮等，也应使用本地固定文案键，不能只翻译页面正文。

## 5. 远程资产词库

### 5.1 标准文件

`SoundEffectPlayer/i18n/zh-cn.json`：

```json
{
  "soundEffectPlayer.data.10001": "环境_震动",
  "soundEffectPlayer.data.10002": "环境_风声_温和",
  "soundEffectPlayer.category.1": "环境"
}
```

`SoundEffectPlayer/i18n/en-us.json`：

```json
{
  "soundEffectPlayer.data.10001": "Environment_Vibration",
  "soundEffectPlayer.data.10002": "Environment_Gentle_Wind",
  "soundEffectPlayer.category.1": "Environment"
}
```

新文件直接使用完整键字典。**不要加 `version`、`namespace`、`locale`、`format`、`translations` 外壳。** 请求时的工具目录、命名空间和语言由项目加载入口确定。

远程资产词库只负责资产名和分类名，固定的播放、搜索等文案仍放在本地 `ui.*` 中。

### 5.2 纯文本语义

远程标准词库中的值按纯文本处理：

```json
{
  "soundEffectPlayer.data.10001": "Wind @ Night {Light} | Loop"
}
```

显示时保留 `@`、`{Light}`、`|`，不会执行插值、链接消息或复数规则。加载器内部会完成必要的消息转义，导出工具无需手工添加 `{'@'}` 等转义。

因此不能把带 `{count}` 的本地固定文案直接搬到远程资产词库并期待相同插值结果。即使都是扁平 JSON，两者的加载语义也不同。

### 5.3 缺译与整表更新

远程某键被删除、值为 `null`、空字符串或全空白，都表示本次表中没有有效译文。非空译文保留其原始内容，不会统一裁掉首尾空格。

```json
{
  "soundEffectPlayer.data.10001": "环境_震动",
  "soundEffectPlayer.data.10002": null
}
```

这不会让 `10002` 自动使用另一种语言。当前语言没有本地同名值时，显示完整键 `soundEffectPlayer.data.10002`。

每份语言文件是当前完整快照，不是增量补丁。上传时应包含该文件负责的所有有效词条；只上传修改过的几项，会使其余旧远程词条消失。

空对象 `{}` 是有效的空表，会清空对应语言和命名空间的旧远程覆盖。它不会清空本地固定文案。

由于标准文件没有语言元数据，加载器不会识别实际译文属于哪种语言。应自行确保英文内容放进 `en-us.json`，中文内容放进 `zh-cn.json`。

## 6. 数据索引

### 6.1 SoundEffectPlayer 标准结构

```json
{
  "data": [
    {
      "id": "10001",
      "nameI18nKey": "soundEffectPlayer.data.10001",
      "duration": "39.862",
      "category": 1,
      "order": 1
    },
    {
      "id": "10002",
      "nameI18nKey": "soundEffectPlayer.data.10002",
      "duration": "69.736",
      "category": 1,
      "order": 2
    }
  ],
  "category": [
    {
      "id": 1,
      "nameI18nKey": "soundEffectPlayer.category.1"
    }
  ]
}
```

顶层 `data` 和 `category` 都是数组。旧的顶层 `"10001": {...}` 音效字典不属于当前标准。

### 6.2 字段定义

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `data` | 数组 | 音效条目列表 |
| `data[].id` | 字符串 | 稳定、唯一的音效 ID |
| `data[].nameI18nKey` | 字符串 | 完整资产名称键 |
| `data[].duration` | 字符串 | 以秒表示的音效时长，如 `"39.862"` |
| `data[].category` | 数字，可选 | 关联 `category[].id`，当前缺失时归为 `0` |
| `data[].order` | 数字，可选 | 分类内的展示顺序 |
| `category` | 数组 | 分类定义列表 |
| `category[].id` | 数字 | 稳定、唯一的分类 ID |
| `category[].nameI18nKey` | 字符串 | 完整分类名称键 |

规范要求资源和分类 ID 唯一，关联的分类存在；有明确分类和顺序时应完整导出这些字段。当前类型仍允许音效的 `category` 和 `order` 缺省，以便页面提供兜底。

索引不直接存储要显示的中文名称，也不在每条资产中嵌入中英文对象。名称修改只改词库；身份、分组或时长变化才改业务字段。

### 6.3 当前页面的读取行为

- `data` 数组在页面内转换为按音效 ID 查询的索引，选择和播放继续使用 ID。
- 根据音效的 `category` 分组，分类名称从顶层 `category` 数组取得。
- 无资源的分类当前不会单独显示。
- 分类按数字 ID 排序；分类内优先按 `order` 排序，缺失顺序的项目放到有序项目之后，再按 ID 的数字顺序比较。
- 当前播放路径仍按 `audio/<id>.mp3` 拼接。导出数据即使额外有 `path`，播放器也不会自动使用它。

后续工具可以增加所需业务字段，但统一沿用数组列表、稳定 ID、`nameI18nKey` 和独立词库的思路。增加新字段时需要同步修改消费者，不能只更新导出 JSON。

`oss.json<SoundEffectData>()` 中的类型参数不会校验运行时数据，它只提供 TypeScript 类型。导出端仍需校验实际字段和关联关系。

## 7. 页面接入

### 7.1 固定文案调用

```vue
<script setup lang="ts">
import { computed } from 'vue';
import { useI18n } from 'vue-i18n';

const { t } = useI18n({ useScope: 'global' });
const title = computed(() => t('soundEffectPlayer.ui.player'));
</script>

<template>
  <section :aria-label="title">
    <h2>{{ title }}</h2>
    <input :placeholder="t('soundEffectPlayer.ui.searchPlaceholder')" />
    <button>{{ t('soundEffectPlayer.ui.play') }}</button>
  </section>
</template>
```

属性需要动态绑定，如 `:title`、`:placeholder`、`:aria-label`。普通属性 `placeholder="t(...)"` 只会显示代码文字。

翻译应在模板、计算属性或事件发生时调用。`const title = t(...)`、`ref(t(...))` 保存的是初始化时的字符串，不会自动随语言变化。

### 7.2 资产名称调用

```ts
import { useI18n } from 'vue-i18n';
import { createCachedText } from '@/i18n/cachedText';

const composer = useI18n({ useScope: 'global' });
const { t } = composer;
const resourceText = createCachedText(composer);
```

模板或 `computed()` 内调用 `resourceText(item.nameI18nKey)`，分类名称同样读取分类项的 `nameI18nKey`。

`createCachedText()` 用于无参数标签：当前语言和词库快照内缓存结果，语言或当前消息对象变化后重建缓存；缺译直接返回键，避免大量缺译资源产生警告。

需要参数的固定句子继续用 `t()`，不要用资源缓存处理 `ui.soundName`、`ui.resultCount` 等文案。

### 7.3 先加载索引，再异步加载翻译

当前音效页使用以下顺序。示例中的相对类型路径适用于音效页面目录：

```ts
import { onMounted, ref } from 'vue';
import { createOss } from '@/utils/oss';
import { loadOssTranslations } from '@/i18n';
import type { SoundEffectData } from './types/SoundEffectData';

const oss = createOss('SoundEffectPlayer');
const soundData = ref<SoundEffectData>({ data: [], category: [] });

onMounted(async () => {
  try {
    soundData.value = await oss.json<SoundEffectData>('data.json');
    void loadOssTranslations('SoundEffectPlayer', 'soundEffectPlayer').then((results) => {
      for (const result of results) {
        if (result.status === 'failed') {
          console.warn('Translation load failed', result.locale, result.error);
        }
      }
    });
  } catch (error) {
    // 这里处理索引请求或解析失败，页面可显示本地 ui.loadFailed 通知。
    console.error('Sound data load failed', error);
  }
});
```

上例增加了加载状态日志供接入时诊断；当前音效页仅触发翻译加载，没有显示专门的翻译失败通知。翻译未加载时数据仍可显示，资源名暂时显示键。

资源翻译请求失败会作为 `failed` 结果返回，不应误报成整个音效索引加载失败。需要检查结果时读取 `status`，而不是只依赖外层 `try/catch`。

## 8. 语言选择和切换

### 8.1 当前支持的语言

`src/i18n/preferences.ts` 定义：

| 内部值 | 显示名称 |
| --- | --- |
| `zh-CN` | 简体中文 |
| `zh-TW` | 繁體中文 |
| `en-US` | English |
| `ja-JP` | 日本語 |
| `ru-RU` | Русский |

当前繁体中文、日文、俄文已开放选择并注册空的本地消息对象，其本地词库尚待补齐；上文中英文文件仍是现有词库示例。远程会请求五种语言各自的文件，未上传或缺少译文时遵循原有缺译规则，不会自动生成翻译。

默认语言为 `zh-CN`，本地存储键为 `ugc-tools.locale`。

### 8.2 初始化优先级

1. 优先使用本地存储中合法的已保存语言。
2. 否则按浏览器语言列表顺序匹配：显式 `zh-Hans` 使用 `zh-CN`；`zh-Hant` 或中文的 `TW`、`HK`、`MO` 地区使用 `zh-TW`；其他中文使用 `zh-CN`；`en`、`ja`、`ru` 分别映射为 `en-US`、`ja-JP`、`ru-RU`。
3. 都未匹配时使用 `zh-CN`。

`src/main.ts` 在挂载前调用 `initializeLocale()`，随后安装全局实例。存储不可访问时仍可完成初始化和切换。

### 8.3 切换入口

```ts
import { setLocale } from '@/i18n';

setLocale('en-US');
```

`setLocale()` 会更新全局语言、页面 `<html lang>` 和本地存储。调用时使用内部语言代码；不支持的值会被忽略。

项目通过空回退链禁用跨语言和地区语言回退。`en-US` 缺译时不会使用 `en`、中文或其他语言。`fallbackFormat` 也已关闭，缺失键不会被当作消息模板再次编译。

切换语言不会自动翻译仍然硬编码的中文，也不会自动给第三方组件注入其专用语言包。每个界面仍需要显式接入。

## 9. 远程加载规则

### 9.1 统一入口

```ts
import { loadOssTranslations } from '@/i18n';

const results = await loadOssTranslations('SoundEffectPlayer', 'soundEffectPlayer');
```

函数参数分别是 OSS 工具目录、命名空间、可选的词库子目录。第三个参数默认 `i18n`，本规范统一采用此目录，不为新页面另起翻译目录名。

一次调用并行请求 `supportedLocales` 中的所有语言，并分别注入全局实例。切换语言时可直接使用已经加载的表，不需要再发同一请求。

### 9.2 返回值

每项结果都带 `namespace` 和 `locale`：

| 状态 | 含义 | 额外字段 |
| --- | --- | --- |
| `loaded` | 文件请求成功、整表校验通过并已应用 | `entries`，有效非空词条数量 |
| `missing` | 最终请求返回 404 | 无 |
| `failed` | 网络、超时、非 404 HTTP 错误、JSON/结构问题或来源冲突 | `error` |

空表也能成功加载，返回 `loaded`、`entries: 0`。某种语言失败不妨碍另一种语言成功。

### 9.3 本地与远程的覆盖关系

同一语言、同一命名空间内，最终消息由本地基底与最新远程表合并，远程同名键优先。

规范把固定文案放在 `ui.*`、资产放在 `data.*`、分类放在 `category.*`，避免发生不必要的同名覆盖。但加载器技术上仍允许远程覆盖本地同名键，目录分工并不是硬性权限隔离。

| 情况 | 当前行为 |
| --- | --- |
| 新表成功加载 | 重建该命名空间的远程快照 |
| 新表删除某键或填空值 | 删除旧远程值；有本地同名值时恢复本地值 |
| 加载 `{}` | 清空该语言和命名空间的远程覆盖，本地文案保留 |
| 最终返回 404 | 同样清空对应远程覆盖 |
| 网络、超时、其他错误或非法 JSON | 保留上次有效翻译 |
| 首次加载失败 | 只能使用本地已注册消息，资源缺译显示键 |

成功更新不会残留旧远程表已经删除的词条。结构校验失败会拒绝整份语言文件，不会只挑有效的几行应用。

加载器在首次应用此命名空间时保存本地基底。因此本地 JSON 应在远程加载前注册，不要在远程加载后随意修改同命名空间并假定刷新会自动保存这些改动为新基底。

### 9.4 刷新、并发与来源约束

- 同一个“命名空间 + 语言”正在加载时，重复调用共享同一个请求。
- 请求完成后再次调用会重新请求；404 不会永久缓存。
- 同一个应用实例中，一个“命名空间 + 语言”只能绑定一个原始 URL；之后切换 URL 会返回 `failed`。
- 主动刷新应再次调用相同入口，不要通过反复更换 URL 参数刷新。加载器已自动追加时间戳。
- 当前没有定时轮询、服务端推送或持久化远程词库缓存。上传后，已打开页面需再次触发加载或刷新才会更新。

### 9.5 底层接口

特殊情况下可指定完整地址：

```ts
import { remoteI18n } from '@/i18n';

const result = await remoteI18n.load({
  namespace: 'soundEffectPlayer',
  locale: 'en-US',
  url: '/ugc-tool-data/SoundEffectPlayer/i18n/en-us.json',
});
```

上例是底层调用方式，不应和不同 URL 的标准入口混用。传入 `locale` 时使用 `en-US`，不要传文件名形式 `en-us`：底层会直接把调用值作为消息注册语言名，不自动规范化大小写。

## 10. 开发与生产地址

### 10.1 默认基础地址

生产环境默认：

```text
https://oss.xiaomol444.xyz/ugc-tool-data
```

音效资源对应：

```text
https://oss.xiaomol444.xyz/ugc-tool-data/SoundEffectPlayer/data.json
https://oss.xiaomol444.xyz/ugc-tool-data/SoundEffectPlayer/i18n/zh-cn.json
https://oss.xiaomol444.xyz/ugc-tool-data/SoundEffectPlayer/i18n/en-us.json
```

这是默认地址规则，不是某次部署的数据快照。开发环境默认使用同源 `/ugc-tool-data`，由项目开发代理转发。

### 10.2 开发 beta 回退

以中文语言表为例：

1. 浏览器请求 `/ugc-tool-data/SoundEffectPlayer/i18n/zh-cn.json`。
2. 开发代理优先读取 OSS `/ugc-tool-data-beta/SoundEffectPlayer/i18n/zh-cn.json`。
3. 只有 beta 返回 **404**，才改读正式 `/ugc-tool-data/SoundEffectPlayer/i18n/zh-cn.json`。
4. beta 返回 200 就使用整份 beta 文件；不会从正式表补充缺失键。

403、5xx、网络异常、200 但 JSON 无效等均不会触发这一文件回退。

索引、语言表、媒体分别按文件回退，可能出现 beta 索引搭配正式语言表。因此索引更新时要检查每种语言是否覆盖新键，不能假定所有文件来自同一个目录。

代理响应头 `X-Ugc-Data-Source` 显示 `beta` 或 `production`。文件来源回退与语言缺译回退是两种机制：前者允许 beta 404 后取正式文件，后者仍禁止英文缺译后取中文。

### 10.3 自定义地址、缓存与超时

| 项目 | 当前行为 |
| --- | --- |
| `VUE_APP_OSS_BASE` | 优先于默认基础地址，使用其他地址时不自动套用默认代理回退 |
| 翻译请求 | 追加 `_t` 时间戳，使用 `cache: 'no-store'` |
| 翻译加载器超时 | 默认 8 秒，超时返回 `failed` |
| 开发代理上游超时 | 默认 30 秒，与翻译加载器限制独立 |
| `oss.json()` 索引请求 | 同样追加 `_t`、禁用缓存，但没有内置同样的 8 秒超时 |
| 开发代理响应 | `Cache-Control: no-store` |

修改开发代理配置或开发环境变量后，应重启开发服务。正式构建默认直连正式目录，不自动读取 beta。

## 11. 搜索、状态和缺译

### 11.1 音效搜索范围

当前音效页面搜索：

- 当前语言的显示名称。
- 音效 ID。

搜索会去掉查询首尾空白并做小写比较。语言或词库变化后，显示名称和搜索结果重新计算。

**同时加载两种语言，不代表当前实现支持跨语言搜索。** 音效页没有同时索引其他语言，也不搜索分类名。后续需要跨语言搜索时，应单独实现并验证，不照搬旧页面的隐含行为。

### 11.2 稳定状态

选中音效保存 `id`，展开分类保存分类 ID，尚未选择用空值表达。显示“未选择”时调用本地 `ui.notSelected`，不要把这个译文作为状态值。

当前音效页切换语言会更新资源名、分类名、固定标签及搜索匹配。筛选结果发生变化时，可见列表随之变化，但所选资源 ID 不因名称翻译而重新分配。

toast 在触发时使用 `t()` 生成当时语言的字符串；已经发出的通知不会因为之后切换语言自动重译。

### 11.3 分类缺译与分类缺失

| 情况 | 显示 |
| --- | --- |
| 分类定义存在、当前语言有译文 | 对应分类译文 |
| 分类定义存在、翻译缺失 | 分类项的完整 `nameI18nKey` |
| 分类为 `0` 且没有名称定义 | 本地 `ui.uncategorized` |
| 其他分类 ID 找不到定义 | 本地 `ui.categoryFallback`，插入分类 ID |

分类定义缺失与翻译缺失不是同一问题。新增语言条目不会创建分类分组，分组仍取决于索引。

### 11.4 自建搜索索引时

资源表经过纯文本转义后保存在消息树中。若直接读取消息树创建搜索索引，可使用 `src/i18n/messageText.ts` 的 `messageSearchText()` 还原项目纯文本转义。

该函数不是任意消息的渲染器，不处理完整参数插值、复数或消息链接。当前音效页按已经解析的显示名称搜索，无需另行使用它。

## 12. 维护与扩展流程

### 12.1 新增或修改固定文案

1. 在当前页面两份本地 JSON 中添加相同键，如 `soundEffectPlayer.ui.download`。
2. 填写中英文，确保参数名称一致。
3. 在模板、计算属性或事件通知中调用 `t()`。
4. 检查属性绑定、英文长度、布局和切换后的更新。
5. 运行本地词库和类型检查。

已有页面增加文案不需要再次注册整个模块，也不需要更改远程资产文件。生产环境需要重新构建和发布前端。

### 12.2 新增音效或分类

1. 为资源分配唯一稳定 ID，并生成 `soundEffectPlayer.data.<ID>`。
2. 在 `data.json` 的 `data` 数组中加入条目，填写 `nameI18nKey` 和业务字段。
3. 若新增分类，在 `category` 数组加入分类 ID 与完整名称键。
4. 在两份远程语言文件中补齐名称与分类译文。
5. 按当前播放器路径约定提供 `audio/<ID>.mp3`。
6. 验证索引、关联、两种语言、搜索和播放。

改译文不改 ID；改排序不改键。不要重新编号整张列表，也不要复用旧 ID 表示完全不同的资源。

### 12.3 新页面接入

1. 选择唯一命名空间，如 `assetBrowser`，本地目录使用同名模块目录。
2. 建立 `locales/assetBrowser/zh-cn.json` 和 `en-us.json`，固定文案用 `assetBrowser.ui.*`。
3. 在 `index.ts` 导入并展开到对应语言，保留 `flatJson: true`。
4. 如有资产，采用 `data`、`category` 数组与 `nameI18nKey`，资产键使用 `assetBrowser.data.*`、`assetBrowser.category.*`。
5. 在工具 OSS 目录下提供 `i18n/zh-cn.json`、`en-us.json`。
6. 页面使用全局 `useI18n()`，索引加载成功后调用 `loadOssTranslations()`。
7. 资源标签响应式解析，固定句子用 `t()`，业务状态保持稳定值。
8. 明确搜索范围，验证空表、缺译、失败和语言切换。

模板只定义通用结构，不要求所有工具拥有音效专属的 `duration` 或音频路径字段。专属字段按业务增加，并同步类型和读取逻辑。

### 12.4 新增一种语言

1. 在 `preferences.ts` 的 `supportedLocales` 增加内部语言值和显示名称。
2. 为所有本地模块添加对应小写文件名 JSON，保持完整键与参数一致。
3. 在 `index.ts` 注册新语言的所有本地消息。
4. 如需根据浏览器自动识别新语言，扩展 `resolveInitialLocale()`；当前识别简繁中文、英语、日语、俄语。
5. 给各工具上传对应语言的远程资产词库。标准加载函数会按新增语言选项自动请求。
6. 更新测试中的语言列表、请求数量和相关断言。
7. 检查语言选择、存储恢复、缺译、搜索和布局。

只上传一个语言文件不会自动出现新语言选项。项目不从远程目录自动发现支持的语言，也没有额外的语言清单文件加载机制。

### 12.5 发布和回归

| 更新 | 所需动作 |
| --- | --- |
| 本地固定文案 | 重新构建发布前端 |
| 远程译文 | 更新对应完整语言文件，重新触发加载后生效 |
| 新资源、分类 | 同步索引、语言文件及所需媒体 |
| 数据字段或结构 | 同步前端读取逻辑和部署数据 |
| beta 语言表 | 上传完整快照，检查实际来源及键覆盖 |

可用表格维护翻译，再按语言导出标准 JSON；接口本身不读取 Excel/CSV。远程更新只是数据更新，不会自动更改代码，也不会即时推送到已打开页面。

## 13. 验证与测试

在仓库根目录执行：

```powershell
npm run type-check
node scripts/test-i18n.cjs
node scripts/test-remote-i18n.cjs
node scripts/test-cached-i18n.cjs
git diff --check
```

| 检查 | 主要内容 |
| --- | --- |
| `type-check` | Vue、TypeScript 和 JSON 导入的类型检查 |
| `test-i18n.cjs` | 初始语言、切换、缺译、本地键覆盖、消息编译、插值和请求 |
| `test-remote-i18n.cjs` | 远程解析、纯文本、命名空间隔离、并发、快照、错误与历史兼容 |
| `test-cached-i18n.cjs` | 资源标签缓存和语言、词库更新后的失效 |
| `git diff --check` | 补丁空白问题 |

修改开发代理时，再执行 `node scripts/test-dev-oss-proxy.cjs`。

现有远程测试包含旧页面的兼容用例，不应把测试里旧格式当作新文件模板。测试主要使用本地或模拟数据，通过测试不代表实际 OSS 的索引和语言文件一定齐全。

手工检查至少覆盖：

- 两种语言的标题、搜索框、按钮、控制标签、空结果和失败提示。
- 已选择音效后切换语言，ID 保持不变，名称即时更新。
- 搜索过程中切换语言，结果符合“当前语言名称 + ID”的规则。
- 资产键缺译时显示完整键，固定 UI 仍正常显示。
- 远程首次失败、已有译文后失败、空表刷新等情况。
- 英文较长文本的布局、占位符、按钮和分类标签。
- 网络面板中的 URL、状态码与 `X-Ugc-Data-Source`。

导出数据还应检查资源 ID 唯一性、分类关联完整性、`nameI18nKey` 与两份词库一致性。这些业务数据检查不等于 TypeScript 类型检查。

## 14. 常见问题

| 现象 | 原因或检查方向 |
| --- | --- |
| 显示 `soundEffectPlayer.data.10001` | 当前语言表未加载、缺少键，或键、命名空间、文件路径错误 |
| 本地 JSON 已添加但显示键 | 没有在 `index.ts` 导入并展开到对应语言 |
| 出现重复命名空间 | 完整键之外又额外包了一层命名空间 |
| 远程 `{count}` 原样显示 | 标准远程资产表按纯文本处理，固定参数句应放本地 |
| 资产的 `@` 或竖线显示异常 | 是否错误使用旧消息格式或手工重复转义 |
| 切换后固定标题没变化 | 是否初始化时保存了 `t()` 的字符串，而非响应式调用 |
| “未选择”一直是中文 | 是否把译文作为业务状态保存，而非显示时翻译 |
| 英文缺译没有自动用中文 | 当前规范禁用跨语言回退，这是预期行为 |
| beta 有文件但缺的词条没从正式补齐 | 回退按文件，只在 beta 404 时发生 |
| 上传后当前页面仍是旧文字 | 没有再次触发加载，应用不轮询或推送 |
| 请求失败后保留旧译文 | 加载器保留上一份有效表，检查 `failed` 和 `error` |
| 删除远程词条后显示本地文案 | 同名本地基底仍存在，按规则恢复本地值 |
| 换 URL 后请求失败 | 同一语言、命名空间只允许一个来源 |
| 中文界面搜不到英文音效名 | 当前音效搜索没有跨语言索引 |
| 加了分类译文但分类不出现 | 分类分组来自索引和资源关系，不由词库创建 |
| 文件叫 `en-us.json`，注册后仍找不到英文 | 底层 `locale` 调用值是否误用 `en-us`，应使用 `en-US` |

排查顺序建议为：确定语言和命名空间 → 检查请求路径与来源 → 检查加载状态和错误 → 检查 JSON 与完整键 → 检查页面调用是否响应式。

## 15. 代码入口与历史兼容边界

| 文件 | 职责 |
| --- | --- |
| [src/main.ts](src/main.ts) | 初始化语言并安装全局 i18n |
| [src/i18n/index.ts](src/i18n/index.ts) | 本地 JSON 注册、全局实例、加载入口与切换 |
| [src/i18n/preferences.ts](src/i18n/preferences.ts) | 支持语言、默认语言、存储键和初始匹配 |
| [src/i18n/remote.ts](src/i18n/remote.ts) | 远程字典校验、加载、快照和失败处理 |
| [src/i18n/cachedText.ts](src/i18n/cachedText.ts) | 无参数资源标签缓存 |
| [src/i18n/messageText.ts](src/i18n/messageText.ts) | 资源纯文本转义与搜索还原 |
| [音效中文固定文案](src/i18n/locales/soundEffectPlayer/zh-cn.json) | 中文 `ui.*` |
| [音效英文固定文案](src/i18n/locales/soundEffectPlayer/en-us.json) | 英文 `ui.*` |
| [SoundEffectPlayer.vue](src/views/SoundEffectPlayer/SoundEffectPlayer.vue) | 本规范的页面参考实现 |
| [SoundEffectData.ts](src/views/SoundEffectPlayer/types/SoundEffectData.ts) | 音效数据索引类型 |
| [src/utils/oss.ts](src/utils/oss.ts) | 基础地址、路径和索引请求 |
| [scripts/dev-oss-proxy.cjs](scripts/dev-oss-proxy.cjs) | 开发环境文件回退 |
| [vue.config.js](vue.config.js) | 开发代理注册、i18n 编译配置 |

当前 `remote.ts` 仍支持旧的 v1 元数据表及其消息模式，这是历史兼容能力。仓库 `examples/i18n/` 和 `migrate-effect-i18n.cjs` 也仍服务于旧特效流程，**不是本规范的标准输出模板**。

后续迁移特效页时，应以本文的音效模式为目标，同步处理索引字段、完整键、远程字典和页面消费逻辑。本文不要求现在修改或移除特效旧实现，也不将其旧结构作为新页面必须兼容的标准。
